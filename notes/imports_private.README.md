# imports_private (LOCAL ONLY)

This folder holds raw/private exports (ChatGPT, Facebook, etc.).
It is gitignored on purpose.

Conventions:
- raw/     = untouched export (zip + extracted)
- working/ = scratch space for parsing/cleanup
