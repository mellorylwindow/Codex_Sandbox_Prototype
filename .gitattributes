# Normalize text files and prefer LF in repo
* text=auto eol=lf

# Shell scripts must be LF
*.sh text eol=lf

# Python files LF
*.py text eol=lf

# Windows command files can stay CRLF if you add any later
*.bat text eol=crlf
*.cmd text eol=crlf
